import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function RecipesScreen() {
  return (
    <PlaceholderScreen
      title="Recipes"
      description="Generate recipes from ingredients via text, image, or voice."
    />
  );
}
